{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://upr.ai/schemas/domain-pattern-discovery-input.json",
  "title": "Domain Pattern Discovery Input",
  "description": "Input schema for domain and email pattern discovery agent",
  "type": "object",
  "required": ["companyName"],
  "properties": {
    "companyName": {
      "type": "string",
      "description": "Company name to discover domain/pattern for",
      "minLength": 1
    },
    "location": {
      "type": ["string", "null"],
      "description": "Geographic location for better search context",
      "default": "United Arab Emirates"
    },
    "useCache": {
      "type": "boolean",
      "description": "Use cached results if available",
      "default": true
    }
  },
  "additionalProperties": false
}
